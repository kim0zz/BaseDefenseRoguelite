/// <summary>
/// Reguły widoczności feedbacku stuna (M9.1) — testowalne bez Unity scene.
/// </summary>
public static class StunFeedbackRules
{
    public static bool ShouldShow(bool isStunned) => isStunned;
}
